# Copy this to .env for local dev, or set as a Vercel Production env var.
# Leave blank locally to use Vite's dev proxy (see vite.config.js) to localhost:8090.
VITE_API_BASE_URL=https://ganeshustav.onrender.com
